export default function Nav() {
  return (
    <nav
      className={"text-gray-600 w-screen flex justify-between dark:bg-black"}
    >
      <span>Shared-Doc</span>
      <button>Sign in</button>
    </nav>
  );
}
